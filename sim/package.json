{
  "name": "sankat-mochan-sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Time-compressed flow simulator: victim SOS -> BLE mesh -> LoRa -> Pi gateway -> AI command post -> responder dispatch -> return path. Fully offline, no backend, no network assets.",
  "license": "MIT",
  "scripts": {
    "predev": "node scripts/copy-assets.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-assets.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@protomaps/basemaps": "^5.7.2",
    "maplibre-gl": "^5.24.0",
    "pmtiles": "^4.4.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
