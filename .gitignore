# macOS
.DS_Store

# Claude Code local settings (machine-specific)
.claude/

# Android / Gradle build artifacts
**/build/
.gradle/
*.iml
.kotlin/
mobile-application/local.properties
local.properties

# IDE
.idea/

# Python (AI pipeline, added later)
__pycache__/
*.pyc
.venv/
venv/
.env
.env.*
!.env.example
!**/.env.example

# Node
node_modules/
**/.vite/

# logs / temp
*.log

# JVM heap dumps (e.g. a crashed Gradle/Studio daemon leaves these behind)
*.hprof

# Compiled AI Hub model artifacts + local model cache (build outputs, not source; too large for git)
backend/export_assets/
backend/pretrained_models/

# Transient test data / benchmark artifacts (not source)
backend/fleurs/
backend/audio/
backend/audio_store/
backend/.postgres.env
backend/backends.json
*.output
/Lottie-icons/
# Durable outbox / local DBs (runtime state, e.g. the Pi edge-link outbox)
*.sqlite
*.db

# External tools
ruflo/

# leftover locked copies from the restructure (delete finetune/ once unlocked)
/finetune/
