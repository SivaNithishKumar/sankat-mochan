<resources>
    <!--
      Base XML theme referenced by the manifest. Compose draws the real UI;
      this only needs to be a no-action-bar material theme so the window and
      splash look right before Compose takes over.
    -->
    <style name="Theme.SankatMochan" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
