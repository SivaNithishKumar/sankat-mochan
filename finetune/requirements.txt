# Sahayak fine-tuning dependencies — pure transformers + PEFT (no Unsloth, no TRL).
# All Apache-2.0 (CLAUDE.md #1).
#
# torch: install the build matching your platform/accelerator FIRST, from https://pytorch.org
#   - CUDA (recommended for real training)         e.g. pip install torch --index-url .../cu121
#   - CPU / Windows-ARM / Surface (pipeline test)  pip install torch
#   - Apple Silicon (MPS)                          pip install torch
#
# Versions are UPPER-BOUNDED, not open-ended, so an unattended `pip install` can't silently
# resolve to a future release that breaks the API surface the trainer uses. These bounds are a
# compatibility window, NOT a GPU-smoke-tested lock — after your first successful CUDA run,
# `pip freeze` the exact versions here to make the run reproducible.
#
# Floor rationale: transformers gained the gemma4 architecture on 2026-04-02
# (huggingface.co/blog/gemma4); 5.6.0 is the first release guaranteed to include it.

transformers>=5.6,<6
datasets>=3.6,<6
peft>=0.16,<1
accelerate>=1.6,<2

# 4-bit QLoRA + the paged 8-bit optimizer. CUDA-only — skip on CPU/MPS/ARM (the trainer
# detects the missing GPU and falls back to an unquantized smoke-test path automatically).
bitsandbytes>=0.46,<1; platform_system == "Linux"
